"""Shared agent runtime (thin). Currently just the per-agent token budget; the scheduler, report artifact,
and registry are extracted here in Phase 2 once a second agent needs them (see the agent-fleet plan)."""
