
# TypeScript incremental build cache: regenerated by every build, so tracking it dirties the
# working tree on each compile and produces diffs nobody reads.
tsconfig.tsbuildinfo
coverage/

# Playwright
test-results/
playwright-report/
.playwright/
